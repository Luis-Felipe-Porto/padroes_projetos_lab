package avaliacao.tarefa06;

import java.util.HashMap;
/*
public class DividasPF implements StrategyDivida{
	
	private HashMap<Cpf, Divida> dividas = new HashMap<>();
	private Cpf cpf;
	
	public DividasPF(HashMap<Cpf, Divida> dividas, Cpf cpf) {
		this.dividas = dividas;
		this.cpf = cpf;
	}
	public void paga(Pagamento pagamento) {
		Divida divida = dividas.get(this.cpf);
		if(divida != null) {
			divida.registra(pagamento);
		}
		
	}
	public void registra(Divida divida) {
		dividas.put(divida.getCpfCredor(),divida);
	}
}*/
