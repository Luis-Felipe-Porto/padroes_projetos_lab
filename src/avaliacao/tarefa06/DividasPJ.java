package avaliacao.tarefa06;

import java.util.HashMap;
/*
public class DividasPJ implements StrategyDivida{
	
	private HashMap<Cnpj, Divida> dividas = new HashMap<>();
	private Cnpj cnpj;
	
	
	public DividasPJ(HashMap<Cnpj, Divida> dividas, Cnpj cnpj) {
		this.dividas = dividas;
		this.cnpj = cnpj;
	}
	public void paga(Pagamento pagamento) {
		Divida divida = dividas.get(this.cnpj);
		if(divida != null) {
			divida.registra(pagamento);
		}
		
	}
	public void registra(Divida divida) {
		dividas.put(divida.getCnpjCredor(),divida);
	}
}
*/